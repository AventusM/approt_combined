import React from "react";
import { View } from "react-native";
import { Text } from "../Generic";

export const ApproEventData = () => {
  return (
    <View>
      <Text>Event data + actions should be displayed under the map</Text>
    </View>
  );
};
